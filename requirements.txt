# Redrob AI — Intelligent Candidate Discovery & Ranking System
# Python 3.10+ required

# Core pipeline (no heavy ML dependencies — runs within 5-min CPU budget)
# No external packages required for the ranking pipeline itself.
# The pipeline uses only Python standard library modules:
#   json, csv, math, datetime, argparse, pathlib, time, dataclasses

# === Web Dashboard (optional — for Streamlit app) ===
streamlit>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0

# === Utility (optional) ===
python-docx>=1.0.0  # Only for reading .docx files
pyyaml>=6.0         # Only for metadata template
